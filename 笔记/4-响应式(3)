## 分支切换

### 问题：

packages\reactivity\examples\03-demo.html

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        padding: 150px;
      }
    </style>
  </head>
  <body>
    <div id="app"></div>
    <button id="flagBtn">update flag</button>
    <button id="nameBtn">update name</button>
    <button id="ageBtn">update age</button>
    <script type="module">
      // 我们自己的
      import { ref, effect } from '../dist/reactivity.esm.js'

      // 官方的
      // import {
      //   ref,
      //   effect,
      // } from '../../../node_modules/vue/dist/vue.esm-browser.prod.js'

      const flag = ref(true)
      const name = ref('memete')

      const age = ref(18)
      effect(() => {
        console.count('effect')
        if (flag.value) {
          app.innerHTML = name.value
        } else {
          app.innerHTML = age.value
        }
      })

      flagBtn.onclick = () => {
        flag.value = !flag.value
      }

      nameBtn.onclick = () => {
        name.value = name.value + Math.random()
      }

      ageBtn.onclick = () => {
        age.value++
      }
    </script>
  </body>
</html>

```

注意看里面的代码，有三个按钮，还有三个事件监听，并且在effect中有操作，对app的展示切换，这个时候页面会出现这样的问题

![image-20260421192507698](./attachments/4-响应式(3).assets/image-20260421192507698.png)

上面的操作可以复现这个问题，问题点在于，当前点击name的时候为什么effect还会执行？因为当前页面显示的是age了，也就是说不依赖name了，既然不依赖了，为什么点击name的时候effect还会执行呢？

先来看一下代码初始的时候会怎么样

![无标题-2026-04-07-1326](./attachments/4-响应式(3).assets/无标题-2026-04-07-1326.png)

在代码初始化的时候会有这样一个结构，因为初始的时候effect中的if会走name这个分支，所以会创建出name的link节点

紧接着，点击flag

无标题-2026-04-07-1327

![无标题-2026-04-07-1327](./attachments/4-响应式(3).assets/无标题-2026-04-07-1327.png)

此时的数据结构就是这个样子

那么上面的问题，明明切换到else分支了，但是在点击name的时候，还是会执行effect，为什么呢？

点击完flag按钮后，再点击一下name按钮，代码的执行流程

![无标题-2026-04-07-1328](./attachments/4-响应式(3).assets/无标题-2026-04-07-1328.png)

从上面可以看出来，这里是会执行effect，因为在点击的时候会执行set，set中会拿到link的所有dep进行执行，因为name还是可以通过link的sub找到effect进行执行

那么怎么解决呢？

首先我们再创建link3节点的时候，也就是点击flag的时候，我们的图是这个样子的

![无标题-2026-04-07-1331](./attachments/4-响应式(3).assets/无标题-2026-04-07-1331.png)

点击完flag，在执行effect回调函数的时候，在这里把deps的nextDep改成了link3节点

![image-20260424142339581](./attachments/4-响应式(3).assets/image-20260424142339581.png)

也就是上面那个图的最终样子了

紧接着我们点击修改name的时候，会通过link2找到effect，导致effect重新执行，影响性能

所以我们可以这样来

看图：

![无标题-2026-04-07-1330](./attachments/4-响应式(3).assets/无标题-2026-04-07-1330.png)

这里的思路是在创建link3的时候，把link3的nextDep指向link2，当depsTail指向的link节点还有nextDep的时候，就把他清理掉

packages\reactivity\src\system.ts

```ts
...

/**
 * 链接ref和effect，建立ref和effect之间的链表关系
 * @param dep ref对象
 * @param sub effect函数（依赖项）
 */
export function link(dep, sub) {

    ...
    
  // 如果activeSub有，那就保存，等更新的时候触发
  const newLink: Link = {
    dep,
    sub,
    nextSub: undefined,
    prevSub: undefined,
    nextDep, // 这里的nextDep是复用失败的
  }

  ...
  
}

...

```



packages\reactivity\src\effect.ts

```ts
...


export class ReactiveEffect {
    
    ...
    
  run() {
        
      ...
      
    /**
     *  这里设置为undefined是做一个标记。如果头节点为undefined，
     * 尾节点也是undefined表示，可以复用link，
     * 但凡头节点不是undefined表示可以复用link了
     */
    this.depsTail = undefined
    try {
      return this.fn()
    } finally {
      endTrack(this)

      // 执行完成之后把activeSub置空，这样在依赖收集的时候就知道没有正在执行的effect函数了
      activeSub = prevSub // 这里恢复之前的effect函数，这样在处理嵌套的逻辑的时候就能正确地收集依赖了
    }
  }

    ...
}

function endTrack(sub) {
  const depsTail = sub.depsTail

  if (depsTail) {
    /**
     * depsTail有，并且depsTail还有nextDep，清理掉依赖关系
     */
    if (depsTail.nextDep) {
      clearTracking(depsTail.nextDep)
      depsTail.nextDep = undefined
    }
  } else if (sub.deps) {
    /**
     * 进入这里的场景是：
     * let e = effect(() => {
     *  if (count > 0) return
     *  count++
     *  if (flag.value) {
     *    app.innerHTML = name.value
     *  } else {
     *    app.innerHTML = age.value
     *  }
     * })
     * 在第一次的时候effect里面会收集到依赖，但是count发生变化的时候，这里的依赖都应该删掉
     */
    clearTracking(sub.deps)
    sub.deps = undefined
  }
}

...
```

这里新增了一个函数，并且有一个判断，如果sub的尾节点还有的话，那么把尾节点后面的清理掉，如果尾节点不存在，那么看一下有没有头节点，如果有头节点，那么全部清理掉。为什么呢？effect执行完了以后depsTail一定会有值的，如果没有值，就只能意味着effect中没有收集到依赖，所以整个sub就都没有节点，所以需要把所有的link节点清理掉。

接着看一下是怎么清理的也就是clearTracking函数

packages\reactivity\src\effect.ts

```ts
/**
 * 清理依赖
 * @param link
 */
function clearTracking(link: Link) {
  while (link) {
    const { dep, nextSub, nextDep, prevSub } = link
    /**
     * 如果上一个节点有，那就把上一个prevSub的下一个节点指向当前节点的下一个
     * 如果没有，那就是头节点，把subs指向下一个就行
     */
    if (prevSub) {
      prevSub.nextSub = nextSub
      link.nextSub = undefined
    } else {
      dep.subs = nextSub
    }

    /**
     * 如果下一个有，那就把next的上一个节点，指向当前节点的上一个节点
     * 如果下一个节点没有，他就是尾节点，那把dep的depsTail指向上一个
     */
    if (nextSub) {
      nextSub.prevSub = prevSub
      link.prevSub = undefined
    } else {
      dep.subsTail = undefined
    }
    link.dep = link.sub = undefined
    link.nextDep = undefined
    link = nextDep
  }
}

```

首先这里接受的参数link是需要删除的节点，这里第一个判断 `  if (prevSub) `解决的问题是这样的

![无标题-2026-04-07-1332](./attachments/4-响应式(3).assets/无标题-2026-04-07-1332.png)

如果说要删除的link2，他还关联其他的link节点，那我们不能直接的删掉link2以及他后面所有的sub，而是要把link2的上一个sub指向link2的下一个sub

![无标题-2026-04-07-1333](./attachments/4-响应式(3).assets/无标题-2026-04-07-1333.png)

应该把ref的依赖关系重新关联

所以有if里面的代码

```ts
    if (prevSub) {
      prevSub.nextSub = nextSub
      link.nextSub = undefined
    }
```

当然如果没有上一个节点，表示这个节点是头节点

```ts
 else {
      dep.subs = nextSub
    }
```

当然还需要处理下一个节点

```ts
    if (nextSub) {
      nextSub.prevSub = prevSub
      link.prevSub = undefined
    } 
```

这里是同样的道理，如果存在下一个节点，那么把下一个节点的上一个节点指向当前节点的上一个节点

同样，如果没有下一个节点，表示当前节点是尾节点

```ts
else {
      dep.subsTail = undefined
    }
```

那么把尾节点设置为undefined

